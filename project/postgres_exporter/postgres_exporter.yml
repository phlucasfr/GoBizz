exclude_databases: 
  - "template0"
  - "template1"

autodiscover:
  enabled: false

queries:
  - name: "Database Size"
    query: "SELECT pg_database.datname AS database, pg_database_size(pg_database.datname) AS size FROM pg_database;"
